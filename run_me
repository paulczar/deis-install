#!/bin/bash


FLEETCTL_ENDPOINT=http://${DEIS_HOST}:4001

cd /root 
git clone https://github.com/deis/deis.git 
cd deis

cat includes.mk | sed 's/^.*You need to set FLEETCTL_TUNNEL/######/'

make run
